--- Ynab/src/modules/finance/pages/Transactions.tsx
+++ Ynab/src/modules/finance/pages/Transactions.tsx
@@ -404,6 +404,7 @@
                 itemCount={sortedTransactions.length}
                 itemSize={68}
                 width="100%"
+                itemData={sortedTransactions}
                 itemKey={(index) => sortedTransactions[index]?.id ?? `fallback-${index}`}
               >
                 {({ index, style }) => {
@@ -621,6 +622,7 @@
                     itemCount={sortedTransactions.length}
                     itemSize={52}
                     width="100%"
+                    itemData={sortedTransactions}
                     itemKey={(index) => sortedTransactions[index]?.id ?? `fallback-${index}`}
                   >
                     {({ index, style }) => {
