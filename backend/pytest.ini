[pytest]
DJANGO_SETTINGS_MODULE = ynab_backend.settings
python_files = test_*.py

# Força o pytest a buscar testes apenas dentro da pasta do aplicativo,
# ignorando completamente os scripts de rascunho soltos na raiz do backend.
testpaths = finance